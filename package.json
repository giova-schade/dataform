{
  "name": "dataform-sandbox",
  "version": "1.0.0",
  "private": true,
  "description": "Sandbox Dataform project",

  "scripts": {
    "compile": "dataform compile",
    "compile:json": "dataform compile --json"
  },

  "dependencies": {
    "@dataform/core": "^3.0.0"
  },

  "devDependencies": {
    "@dataform/cli": "^3.0.0-beta"
  }
}
